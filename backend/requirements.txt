# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # lets FastAPI receive uploaded files
pydantic-settings==2.7.1        # load config from environment variables

# Pin below 1.0: huggingface-hub 1.x removed the [inference] extra that
# llama-index-embeddings-huggingface asks for, which makes pip backtrack forever.
huggingface-hub>=0.23,<1.0
sentence-transformers>=2.6,<4.0

# --- LlamaIndex (the RAG orchestration framework) ---
llama-index-core==0.12.10
llama-index-embeddings-huggingface==0.4.0   # free, local embeddings
llama-index-llms-groq==0.3.1                # free Llama-3 models via Groq API
llama-index-vector-stores-chroma==0.4.1     # store embeddings in ChromaDB
llama-index-readers-file==0.4.3             # PDF / txt parsing

# --- Vector database ---
chromadb==0.5.23

# --- Bonus: chat-session storage ---
pymongo==4.10.1

# --- Bonus: Dappier web access tool ---
dappier==0.3.3

# --- Utilities ---
python-dotenv==1.0.1
